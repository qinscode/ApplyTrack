import { type ILayoutConfig } from '@/providers'

// Defining the layout configuration specific to layout
const MainLayoutConfig: ILayoutConfig = {
  name: 'layout', // Unique name identifier for this layout
  options: {}
}

export { MainLayoutConfig }
